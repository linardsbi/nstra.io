---
import Action from "@/components/Action.astro";
import type { FooterData } from "@/config/landing.interface";

interface Props {
  data: FooterData;
}

const { data } = Astro.props;
---

<footer
  class="flex flex-col justify-center text-center gap-y-8 pt-8 bg-black text-white w-full"
>
  <section>
    <Action as="a" type="text" class="text-xl md:text-2xl" href="/">
      {data.logo}
    </Action>
  </section>
  <section>
    <p class="md:text-lg">
      {data.description}
    </p>
  </section>
  <section
    class="flex flex-row flex-wrap justify-center gap-x-3 px-4 pb-6 md:px-0 md:text-xl"
  >
    {
      data.links.map((link) => (
        <Action as="a" type="text" href={link.href}>
          {link.label}
        </Action>
      ))
    }
  </section>
</footer>
